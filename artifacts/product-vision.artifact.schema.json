{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "gsmarc://safe/artifacts/product-vision.artifact/v1",
  "$ref": "gsmarc://safe/harness/contracts/artifact/v1",
  "title": "product-vision artifact schema",
  "type": "object",
  "additionalProperties": true,
  "x-artifact": {
    "id": "product-vision",
    "kind": "product-vision",
    "template": "templates/layers/program/actors/product-manager/artifacts/product-vision.artifact-template.md",
    "pathPatterns": [
      "products/*/product-vision.md"
    ],
    "renderOnly": false
  },
  "required": [
    "__path",
    "__kind",
    "__artifact_id",
    "__frontmatter_present"
  ],
  "properties": {
    "__path": {
      "description": "Workspace-relative path of the artifact file, injected by the orchestration harness (not authored in frontmatter).",
      "type": "string",
      "minLength": 1
    },
    "__kind": {
      "description": "Artifact-kind discriminator injected by the harness; must equal x-artifact.kind.",
      "const": "product-vision"
    },
    "__artifact_id": {
      "description": "Stable artifact id injected by the harness, derived from the file and frontmatter.",
      "type": "string",
      "minLength": 1
    },
    "__frontmatter_present": {
      "description": "Harness assertion that YAML frontmatter was found and parsed; always true for a valid artifact.",
      "type": "boolean"
    },
    "__sections": {
      "description": "Artifact body sections keyed by their `## ` heading \u2014 one property per template section; the value is the raw Markdown under that heading. Authored artifacts may add or omit sections, so this is advisory (additionalProperties is permitted) and no section is individually required.",
      "type": "object",
      "properties": {
        "Vision statement": {
          "description": "For <customers> who <need>, <product> is a <category> that <value>; unlike <alt>, <differentiator>.",
          "type": "string"
        },
        "Target customers & jobs-to-be-done": {
          "description": "...",
          "type": "string"
        },
        "Key differentiators": {
          "description": "...",
          "type": "string"
        },
        "Strategic themes served": {
          "description": "Links to `portfolio/strategic-themes.md`.",
          "type": "string"
        },
        "NFR + runway anchors": {
          "description": "Pointers to `architecture/nfrs.md` and `architecture/runway.md` that constrain the Vision.",
          "type": "string"
        }
      },
      "additionalProperties": true
    },
    "__body": {
      "description": "Raw Markdown body of the artifact (everything after the frontmatter), injected by the harness.",
      "type": "string"
    },
    "title": {
      "description": "Human-readable title of the artifact.",
      "type": "string",
      "minLength": 1
    },
    "workItemRelations": {
      "description": "Typed work-item relations (dependsOn / enables / relatedTo / supersedes).",
      "$ref": "gsmarc://safe/artifacts/framework-definitions/v1#/definitions/workItemRelations"
    },
    "wsjf": {
      "description": "Weighted Shortest Job First scoring block.",
      "$ref": "gsmarc://safe/artifacts/framework-definitions/v1#/definitions/wsjf"
    },
    "risk": {
      "description": "Risk level \u2014 the consequence of being wrong: low | medium | critical.",
      "enum": [
        "low",
        "medium",
        "critical"
      ]
    },
    "complexity": {
      "description": "Complexity \u2014 cognitive/structural difficulty: simple | involved | complex.",
      "enum": [
        "simple",
        "involved",
        "complex"
      ]
    },
    "created": {
      "description": "Authoring date (YYYY-MM-DD).",
      "type": "string",
      "minLength": 1
    },
    "openItems": {
      "description": "Clarification + challenge ledger (a blocking open entry halts the relevant gate).",
      "$ref": "gsmarc://safe/artifacts/framework-definitions/v1#/definitions/openItems"
    },
    "cost": {
      "description": "Token-cost accounting block (see the cost definition).",
      "$ref": "gsmarc://safe/artifacts/framework-definitions/v1#/definitions/cost"
    },
    "github": {
      "description": "GitHub Projects sync envelope, filled by portfolio/_sync (null when authoring).",
      "$ref": "gsmarc://safe/artifacts/framework-definitions/v1#/definitions/githubSync"
    },
    "sources": {
      "description": "Derivation provenance (narrowed subtype of the base contract's sources): refs to the upstream artifact(s) this product-vision was built from. Allowed source kinds: portfolio-vision, strategic-themes.",
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "string",
        "pattern": "(^|/)([a-z0-9-]+\\.)?(portfolio-vision|strategic-themes)\\.md$"
      }
    }
  }
}