{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "gsmarc://safe/harness/contracts/log-entry/v1",
  "title": "Harness log entry contract",
  "description": "One append-only log entry for one completed harness function invocation. The persisted log entry integrates the function report with its wall-clock write time; the report contains its session context and function-specific payload.",
  "type": "object",
  "unevaluatedProperties": false,
  "oneOf": [
    {
      "type": "object",
      "required": [
        "timestamp",
        "report"
      ],
      "properties": {
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "Wall-clock time the log entry was appended."
        },
        "report": {
          "$ref": "api/register-session.output.schema.json",
          "description": "The function report persisted by this entry."
        }
      },
      "unevaluatedProperties": false,
      "description": "Log entry for a register-session report."
    },
    {
      "type": "object",
      "required": [
        "timestamp",
        "report"
      ],
      "properties": {
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "Wall-clock time the log entry was appended."
        },
        "report": {
          "$ref": "api/resolve-workflow-instructions.output.schema.json",
          "description": "The function report persisted by this entry."
        }
      },
      "unevaluatedProperties": false,
      "description": "Log entry for a resolve-workflow-instructions report."
    },
    {
      "type": "object",
      "required": [
        "timestamp",
        "report"
      ],
      "properties": {
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "Wall-clock time the log entry was appended."
        },
        "report": {
          "$ref": "api/resolve-workflow-skills.output.schema.json",
          "description": "The function report persisted by this entry."
        }
      },
      "unevaluatedProperties": false,
      "description": "Log entry for a resolve-workflow-skills report."
    },
    {
      "type": "object",
      "required": [
        "timestamp",
        "report"
      ],
      "properties": {
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "Wall-clock time the log entry was appended."
        },
        "report": {
          "$ref": "api/resolve-step.output.schema.json",
          "description": "The function report persisted by this entry."
        }
      },
      "unevaluatedProperties": false,
      "description": "Log entry for a resolve-step report."
    },
    {
      "type": "object",
      "required": [
        "timestamp",
        "report"
      ],
      "properties": {
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "Wall-clock time the log entry was appended."
        },
        "report": {
          "$ref": "api/resolve-step-model.output.schema.json",
          "description": "The function report persisted by this entry."
        }
      },
      "unevaluatedProperties": false,
      "description": "Log entry for a resolve-step-model report."
    },
    {
      "type": "object",
      "required": [
        "timestamp",
        "report"
      ],
      "properties": {
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "Wall-clock time the log entry was appended."
        },
        "report": {
          "$ref": "api/check-step-preconditions.output.schema.json",
          "description": "The function report persisted by this entry."
        }
      },
      "unevaluatedProperties": false,
      "description": "Log entry for a check-step-preconditions report."
    },
    {
      "type": "object",
      "required": [
        "timestamp",
        "report"
      ],
      "properties": {
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "Wall-clock time the log entry was appended."
        },
        "report": {
          "$ref": "api/resolve-step-instructions.output.schema.json",
          "description": "The function report persisted by this entry."
        }
      },
      "unevaluatedProperties": false,
      "description": "Log entry for a resolve-step-instructions report."
    },
    {
      "type": "object",
      "required": [
        "timestamp",
        "report"
      ],
      "properties": {
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "Wall-clock time the log entry was appended."
        },
        "report": {
          "$ref": "api/resolve-step-skills.output.schema.json",
          "description": "The function report persisted by this entry."
        }
      },
      "unevaluatedProperties": false,
      "description": "Log entry for a resolve-step-skills report."
    },
    {
      "type": "object",
      "required": [
        "timestamp",
        "report"
      ],
      "properties": {
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "Wall-clock time the log entry was appended."
        },
        "report": {
          "$ref": "api/check-step-authorization.output.schema.json",
          "description": "The function report persisted by this entry."
        }
      },
      "unevaluatedProperties": false,
      "description": "Log entry for a check-step-authorization report."
    },
    {
      "type": "object",
      "required": [
        "timestamp",
        "report"
      ],
      "properties": {
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "Wall-clock time the log entry was appended."
        },
        "report": {
          "$ref": "api/check-step-artifact.output.schema.json",
          "description": "The function report persisted by this entry."
        }
      },
      "unevaluatedProperties": false,
      "description": "Log entry for a check-step-artifact report."
    },
    {
      "type": "object",
      "required": [
        "timestamp",
        "report"
      ],
      "properties": {
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "Wall-clock time the log entry was appended."
        },
        "report": {
          "$ref": "api/check-step-postconditions.output.schema.json",
          "description": "The function report persisted by this entry."
        }
      },
      "unevaluatedProperties": false,
      "description": "Log entry for a check-step-postconditions report."
    }
  ]
}