{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://poesis.cloud/safe-agentic-framework/schemas/epic-enabler.artifact.schema.json",
  "$ref": "https://poesis.cloud/safe-agentic-framework/artifact.schema.json",
  "title": "epic-enabler artifact schema",
  "type": "object",
  "additionalProperties": true,
  "x-artifact": {
    "id": "epic-enabler",
    "kind": "epic-enabler",
    "template": "templates/layers/portfolio/actors/enterprise-architect/artifacts/epic-enabler.artifact-template.md",
    "pathPatterns": [
      "portfolio-backlog/*/*.epic-enabler.md"
    ],
    "sections": {
      "required": [
        "Overview"
      ],
      "optional": [
        "Program Themes",
        "Features",
        "Implementation Notes",
        "Open Items"
      ],
      "maxDepth": 3,
      "allowUnknown": false
    },
    "type": "enabler"
  },
  "required": [
    "__path",
    "__kind",
    "__artifact_id",
    "__frontmatter_present",
    "status",
    "type",
    "title",
    "strategic_theme",
    "business_owner",
    "enterprise_architect",
    "products",
    "work_item_relations",
    "wsjf",
    "mvp_features",
    "created",
    "open_items",
    "cost",
    "github",
    "enabler_type"
  ],
  "properties": {
    "__path": {
      "description": "Workspace-relative path of the artifact file, injected by the orchestration harness (not authored in frontmatter).",
      "type": "string",
      "minLength": 1
    },
    "__kind": {
      "description": "Artifact-kind discriminator injected by the harness; must equal x-artifact.kind.",
      "const": "epic-enabler"
    },
    "__artifact_id": {
      "description": "Stable artifact id injected by the harness, derived from the file and frontmatter.",
      "type": "string",
      "minLength": 1
    },
    "__frontmatter_present": {
      "description": "Harness assertion that YAML frontmatter was found and parsed; always true for a valid artifact.",
      "const": true
    },
    "__sections": {
      "description": "Artifact body sections keyed by their `## ` heading \u2014 one property per template section; the value is the raw Markdown under that heading. Authored artifacts may add or omit sections, so this is advisory (additionalProperties is permitted) and no section is individually required.",
      "type": "object",
      "properties": {
        "Enablement statement": {
          "description": "Use exactly one hypothesis block in this canonical form",
          "type": "string"
        },
        "Runway outcome & leading indicators": {
          "description": "`Leading indicator: <early measurable signal>`",
          "type": "string"
        },
        "In scope / out of scope": {
          "description": "Out (non-goals)",
          "type": "string"
        },
        "Enablement rationale": {
          "description": "Why this runway investment matters now, and what delivery or operability risk it removes.",
          "type": "string"
        },
        "Enabler properties": {
          "description": "Consuming products / Features",
          "type": "string"
        },
        "Enterprise-Architect runway (EA hat)": {
          "description": "Cross-product architectural implications, NFR backbone, shared enablers, and which products must build runway.",
          "type": "string"
        },
        "Affected products & Feature seeds": {
          "description": "Use one row per product seed in this form",
          "type": "string"
        },
        "MVP & pivot/persevere": {
          "description": "The smallest enabler Feature set (`mvp_features`) that makes the blocked delivery path viable.",
          "type": "string"
        },
        "Dependencies / constraints": {
          "description": "Other Epics, external systems, regulations.",
          "type": "string"
        },
        "Work-item relations": {
          "description": "Use only relation types that materially shape sequencing or runway intent.",
          "type": "string"
        },
        "Open items": {
          "description": "The human-readable companion to the `open_items:` frontmatter ledger (the [open-item ledger](../../orchestrator/SKILL.md#open-item-ledger)) \u2014 this enabler Epic's **clarifications** (proactive unknowns surfaced by the CE Discovery turn) and **challenges** (reactive findings from peer review)...",
          "type": "string"
        }
      },
      "additionalProperties": true
    },
    "__body": {
      "description": "Raw Markdown body of the artifact (everything after the frontmatter), injected by the harness.",
      "type": "string"
    },
    "status": {
      "description": "funnel | reviewing | analyzing | portfolio-backlog | implementing | done | blocked",
      "enum": [
        "funnel",
        "reviewing",
        "analyzing",
        "portfolio-backlog",
        "implementing",
        "done",
        "blocked"
      ]
    },
    "type": {
      "description": "Artifact type discriminator.",
      "enum": [
        "enabler"
      ]
    },
    "title": {
      "description": "Human-readable title of the artifact.",
      "type": "string",
      "minLength": 1
    },
    "work_item_relations": {
      "description": "Typed work-item relations (depends_on / enables / related_to / supersedes).",
      "$ref": "https://poesis.cloud/safe-agentic-framework/schemas/framework-definitions.schema.json#/definitions/workItemRelations"
    },
    "wsjf": {
      "description": "Weighted Shortest Job First scoring block.",
      "$ref": "https://poesis.cloud/safe-agentic-framework/schemas/framework-definitions.schema.json#/definitions/wsjf"
    },
    "created": {
      "description": "Authoring date (YYYY-MM-DD).",
      "type": "string",
      "minLength": 1
    },
    "open_items": {
      "description": "clarification + challenge ledger \u2014 see orchestrator \"Open-item ledger\". Each entry: { id, kind, raised_by, owner, blocking, status, \u2026 }; kind: clarification | challenge. status: open | resolved | withdrawn. A blocking+open entry HALTS the \u2605 Epic Gate.",
      "$ref": "https://poesis.cloud/safe-agentic-framework/schemas/framework-definitions.schema.json#/definitions/openItems"
    },
    "cost": {
      "description": "Token-cost accounting block (see the cost definition).",
      "$ref": "https://poesis.cloud/safe-agentic-framework/schemas/framework-definitions.schema.json#/definitions/cost"
    },
    "github": {
      "description": "GitHub Projects sync envelope, filled by portfolio/_sync (null when authoring).",
      "$ref": "https://poesis.cloud/safe-agentic-framework/schemas/framework-definitions.schema.json#/definitions/githubSync"
    },
    "strategic_theme": {
      "description": "Strategic theme this Epic advances.",
      "type": "string",
      "minLength": 1
    },
    "business_owner": {
      "description": "Business-Owner-hat approver for this Epic.",
      "type": "string",
      "minLength": 1
    },
    "enterprise_architect": {
      "description": "Enterprise-Architect-hat owner of the architectural runway.",
      "type": "string",
      "minLength": 1
    },
    "products": {
      "description": "Product slugs this Epic spans (cross-product is allowed only at the Epic tier).",
      "type": "array",
      "items": {
        "type": "string"
      },
      "minItems": 1
    },
    "mvp_features": {
      "description": "Feature ids that constitute the MVP (the pivot/persevere test).",
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "enabler_type": {
      "description": "architectural | infrastructure | compliance",
      "enum": [
        "architectural",
        "infrastructure",
        "compliance"
      ]
    },
    "sources": {
      "description": "Derivation provenance (narrowed subtype of the base contract's sources): refs to the upstream artifact(s) this epic-enabler was built from. Allowed source kinds: strategic-themes, architecture-vision.",
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "string",
        "pattern": "(^|/)([a-z0-9-]+\\.)?(strategic-themes|architecture-vision)\\.md$"
      }
    }
  }
}